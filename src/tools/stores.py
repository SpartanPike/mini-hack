from math import radians, sin, cos, asin, sqrt

def get_nearby_stores(lat, lon):
    stores = [
        {"id": "store_1", "name": "Starbucks", "lat": lat + 0.0003, "lon": lon},
    ]
    return stores
